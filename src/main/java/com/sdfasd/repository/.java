1. Create a new maven project.

2. Add the Spring Boot starter parent and dependencies to the project.

3. Create the application class which contains the main() method.

4. Create the application.properties file to configure the application properties.

5. Create the repositories package and add the necessary Spring Data JPA repositories.

6. Create the controller package and add the necessary Spring MVC controllers.

7. Create the service package and add the necessary Spring service classes.

8. Create the model package and add the necessary entity classes.

9. Create the package com.sdfasd.repository and add the necessary Spring Data JPA Repository classes.